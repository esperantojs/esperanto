(function () {

	'use strict';

	/* jshint esnext:true */

	var a = 'a';
	var b = 'b';
	var exporter = 'DEF';

	/* jshint esnext:true */

	assert.equal(a, 'a');
	assert.equal(b, 'b');
	assert.equal(exporter, 'DEF');

}).call(global);