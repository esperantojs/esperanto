(function () {

	'use strict';

	var exporter = require('./exporter');
	var exporter = require('./exporter');
	
	/* jshint esnext:true */
	
	assert.equal(exporter.default(), 1);
	assert.equal(exporter.default(), 1);

}).call(global);